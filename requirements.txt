# NHAI Drishti — Python 3.11 | Windows 11 | TensorFlow 2.15
# Install: pip install -r requirements.txt

# Core numeric / image
numpy==1.26.4
opencv-python==4.11.0.86

# AI / ML
mediapipe==0.10.14
tensorflow==2.15.0
protobuf==4.25.3
flatbuffers==25.2.10

# HTTP / AWS sync (optional — only needed if AWS_API_ENDPOINT is configured)
requests==2.32.4
boto3==1.38.41
botocore==1.38.41
s3transfer==0.13.0

# Utilities
Pillow==11.2.1
python-dateutil==2.9.0.post0
PyYAML==6.0.2

# Benchmark only (not required for main.py)
# psutil
# matplotlib
# pandas
# scipy
# tqdm
